version: 0.2
  phases:
  install:
      runtime-versions:
          python: 3.8
  build:
    commands:
      - mkdir temp || true
      - mkdir build
      - pip3.8 install -r app/requirements.txt -t temp
      - ls temp
#artifacts:
#  files:
#    - 'brownbag.zip'
#  name: brownbag-$(date +%Y-%m-%d-%H%M%S).zip
#  base-directory: build
#  discard-paths: yes
#environment:
#  type: "LINUX_CONTAINER"
#  image: "aws/codebuild/standard:4.0"
